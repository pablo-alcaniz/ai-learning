import model

# train(learning_rate, iterations, n1, n2, save_name):

learning_rate = 0.1
iterations = 500
n1 = 20
n2 = 10
save_name = "01-model"

model.train(learning_rate, iterations, n1, n2, save_name)
